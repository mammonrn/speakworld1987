'use strict';

/**
 * ยังไม่ได้พัฒนา — ที่ว่างสำหรับขั้นตอนเลือกภาษาปลายทาง
 * (คาซัค / อังกฤษ / จีน / พม่า) ผ่านปุ่ม inline keyboard
 * และเก็บภาษาที่ผู้ใช้เลือกไว้ต่อ chat
 *
 * เมื่อพัฒนาเสร็จ ให้เรียก register() จาก src/handlers/index.js
 *
 * @param {import('telegraf').Telegraf} _bot
 */
function register(_bot) {
  // TODO: ใส่คำสั่ง /language และ callback query สำหรับเลือกภาษา
}

module.exports = { register };
