'use strict';

/**
 * ยังไม่ได้พัฒนา — ที่ว่างสำหรับการแปลข้อความ
 * ไทย → ภาษาปลายทาง และแปลกลับอัตโนมัติ
 *
 * เมื่อพัฒนาเสร็จ ให้เรียก register() จาก src/handlers/index.js
 *
 * @param {import('telegraf').Telegraf} _bot
 */
function register(_bot) {
  // TODO: เชื่อมต่อ Translation API และตอบกลับผลการแปลทั้งสองทาง
}

module.exports = { register };
